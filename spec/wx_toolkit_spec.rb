require "spec_helper"

describe WxToolkit do
  it "has a version number" do
    expect(WxToolkit::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
